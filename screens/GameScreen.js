import React from 'react'
import { StyleSheet, Text, View, Button } from 'react-native'

const GameScreen = () => {








    return (
        <View>
            <Text>Test</Text>
            <Button>Test
                onPress={console.log(_inputs)}
            </Button>
        </View>
    )
}

export default GameScreen

const styles = StyleSheet.create({})
